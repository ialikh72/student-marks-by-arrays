# Marks Average Using Arrays in C#

This is a simple C# console application that demonstrates the use of
arrays, functions, and `out` parameters.

## 📌 Program Description
- Takes marks of 5 subjects from the user
- Stores them in a float array
- Calculates the total marks
- Calculates the average marks
- Displays total and average in `Main()`

## 🧠 Concepts Used
- Arrays in C#
- Functions
- `out` parameter
- `for` loop
- Console input/output

## ▶ How It Works
1. `Main()` calls the `marks()` function
2. The function:
   - Takes input for 5 subject marks
   - Stores them in an array
   - Calculates total marks
   - Returns the average
3. `Main()` prints total and average.
